# OTR Market Operation 4.4.1

Adds OTR branding to the browser tab for the permanent Railway/custom-domain deployment.

- Reuses the existing OTR logo.
- Adds a 64x64 PNG favicon.
- Adds a 180x180 Apple touch icon.
- Adds explicit favicon links to the dashboard head.
- Uses cache-busting version `4.4.1`.
